(function(){'use strict';function esc(v){return String(v==null?'':v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;')}async function load(){var box=document.getElementById('curriculum-reviews-due');if(!box)return;try{var r=await ccAuth.fetchAuthed('/api/curriculum-progress?action=reviews-due');var data=await r.json();if(!r.ok||data.enabled!==true){box.hidden=true;return}window.curriculumProgressBetaEnabled=true;var rows=data.reviews||[];box.hidden=false;box.innerHTML='<div><h2>Reviews due</h2><p>'+(rows.length?rows.length+' past booked lesson'+(rows.length===1?' needs':'s need')+' a curriculum review.':'You’re up to date with booked-lesson reviews.')+'</p></div>'+(rows.length?'<div class="cp-banner-list">'+rows.slice(0,8).map(function(x){return '<div class="cp-banner-row"><div><strong>'+esc(x.learner_name)+'</strong><br><small>'+esc(x.scheduled_date)+' · '+esc(String(x.start_time).slice(0,5))+'</small></div><a class="cp-btn" href="/instructor/review-lesson?booking_id='+x.booking_id+'">Review lesson</a></div>'}).join('')+'</div>':'')}catch(e){box.hidden=true}}document.addEventListener('DOMContentLoaded',load)})();
